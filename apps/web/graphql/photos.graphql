query Photos($keyword: String!) {
  photos(where: {_or: [{author: {_ilike: $keyword}}, {description: {
    _ilike: $keyword
  }}]}) {
    id
    author
    url
    description
    created_at
    updated_at
  }
}
