query Photos($keyword: String!) {
  photos(where: {_or: [{author: {_like: $keyword}}, {description: {
    _like: $keyword
  }}]}) {
    id
    author
    url
    description
    created_at
    updated_at
  }
}
